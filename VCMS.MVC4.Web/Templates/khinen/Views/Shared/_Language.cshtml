@{
    var i = 1;
    <div class="language pull-right">
        @*<label>@Html.Locale("language"): </label>
            <select id="selectLang" class="ctn">
                @foreach (var item in SiteConfig.Languages)
                {
                <option value="@item.Id" @(SiteConfig.LanguageId= =item.Id ? "selected" : "" )>@item.DisplayName</option>
                }
            </select>*@
        @foreach (var item in SiteConfig.Languages)
        {
            <a href="@Url.Action("Index", "Language" , new { id=item.Id })" class="lg-@i @(SiteConfig.LanguageId == item.Id ? " active" : "" )" title="@item.DisplayName">
                @Html.Image(SiteConfig.Image("lang_" + item.Code + ".png"), new ImageOptions { Width = 35, Height = 35, Lazy = false }, new { alt = item.Code })
            </a>
            i++;
        }
        @*<div id="google_translate_element" style="float: left;"></div>
            <script type="text/javascript">
                function googleTranslateElementInit() {
                    new google.translate.TranslateElement({ pageLanguage: 'en', layout: google.translate.TranslateElement.InlineLayout.HORIZONTAL, autoDisplay: false }, 'google_translate_element');
                }
            </script>
            <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>*@
    </div>
}

@*
    <script type="text/javascript">
        $(document).ready(function () {
            $("#selectLang").change(function () {
                var val = $(this).val();
                if (val != "") {
                    window.location.href = '/Language/Index/' + val;
                }
            });
        });
    </script>*@