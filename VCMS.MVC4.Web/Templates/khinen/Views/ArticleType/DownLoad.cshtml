@model ArticleType
@{
    ViewBag.Title = Model.Name;
    ViewBag.Breadcrumb = new BreadcrumbModel { Items = new List<BreadcrumbItem> { new BreadcrumbItem { Text = Model.Name, Url = null, IsLast = true } } };
}
@Html.Action("ViewByType", "Article", new { typeId = Model.Id, viewPath = "DownLoad/_List", pageSize = 10, sortOrder = ArticleSortOrder.SORT_ORDER, direction = VCMS.MVC4.Data.SortDirection.DESCENDING, includeFlags = ArticleIncludeFlags.FILES | ArticleIncludeFlags.ARTICLE_TYPE | ArticleIncludeFlags.PROPERTIES })