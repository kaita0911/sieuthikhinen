@using VCMS.MVC4.Web
@using VCMS.MVC4.Data
@model VCMS.MVC4.Data.Language

@{
    Layout = "";
}

<div class="modal-dialog">
    <div class="form no-boder-radius">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
            <h4 class="modal-title" id="myModalLabel">Language<span id="success"></span></h4>
        </div>
        @using (Ajax.BeginForm("Edit", "Language", new AjaxOptions { HttpMethod = "post", OnSuccess = "Success" }))
        {
            @Html.ValidationSummary(true)

            <fieldset class="modal-body">
                @Html.HiddenFor(model => model.Id)
                <div class="editor-label">
                    @Html.LabelFor(model => model.Code)
                </div>
                <div class="editor-field">
                    @Html.EditorFor(model => model.Code)
                    @Html.ValidationMessageFor(model => model.Code)
                </div>
                <div class="editor-label">
                    @Html.LabelFor(model => model.DisplayName)
                </div>
                <div class="editor-label">
                    @Html.EditorFor(model => model.DisplayName)
                    @Html.ValidationMessageFor(model => model.DisplayName)
                </div>
                <div class="editor-label">
                    @Html.LabelFor(model => model.Locale)
                </div>
                <div class="editor-label mg-bottom-10">
                    @Html.EditorFor(model => model.Locale)
                    @Html.ValidationMessageFor(model => model.Locale)
                </div>
                @*<div class="editor-field">
                    @Html.Label("Thứ tự")
                    @Html.TextBoxFor(model => model.SortOrder)
                </div>*@
                @*<div class="editor-label">
                    @Html.CheckBoxFor(model => model.IsDefault)
                    @Html.Label("Đặt làm mặc định", new { @style = "display:inline" })
                </div>*@
                <div id="error" class="editor-label">

                </div>
            </fieldset>
            <div class="modal-footer">
                <button type="button" class="btn delete popup" data-dismiss="modal"><i class="fa fa-times"></i> Close</button>
                <button type="submit" class="btn btn-primary"><i class="fa fa-save"></i> Save changes</button>
            </div>
        }
    </div>
</div>